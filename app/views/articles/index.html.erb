<h1>All articles</h1>

  <% @articles.each do |article| %>
    <h2> <%= link_to article["title"], article_path(article["id"]) %> </h2>
    <p><%= article.content %></p>
  <% end %>

<%= link_to ("<i class='fa-solid fa-plus'></i> Add new article").html_safe, new_article_path %>
